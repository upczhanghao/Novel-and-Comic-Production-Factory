# 系统审查报告 — v2.1.0

**日期**: 2026-05-21
**审查范围**: 后端 12 个 router + 前端 14 个 view + 所有 component / store / composable
**审查方法**: 4 路并行 agent 全栈扫描,从「工作流连续性 / 易用性与可发现性 / 功能合理性」三个维度交叉评估

---

## 🔴 严重 (P0) — 工作流断裂、数据丢失风险、死路

| # | 模块 | 位置 | 问题 | 建议修复 |
|---|------|------|------|----------|
| S1 | 工坊 | `useWorkshopState.ts:825-829` | `watch(batch.running)` 自动触发 `doExportNovel()`;**用户取消**批量生成也会下载半成品 | 由服务端发送 "completion" 信号,或加入 "完成后自动导出" 复选框 |
| S2 | 工坊 | `useWorkshopState.ts:519-527` 与 `:469-473` | Ctrl+S 跳到已废弃的 `saveArchitecture()`,仅吐出 "架构文件为只读" 误导 toast | 路由到 5 个子组件 save;或移除 architecture 分支 |
| S3 | 工坊 | `useWorkshopState.ts:303-351` (`reloadProjectContent`) | 项目切换/页面刷新后不恢复 `chapter.result` / `finalize.result` / `humanize.*` / `outlineBatchResult`,**已生成章节无法编辑** | `ChapterStep.vue` 加 "📂 加载第 N 章" 按钮调 `generateApi.getChapter(num)` |
| S4 | 工坊 | `DraftRestoreBanner.vue` + `useDraftAutosave.ts:46-52` | 草稿恢复无条件覆盖 9 个字段,**会用旧 localStorage 静默覆盖服务端较新数据** | 比较 snapshot.ts 与 `projectStore.activeProjectData.updated_at`,仅在 snapshot 更新时显 banner |
| S5 | 工坊 | `ChapterStep.vue:13-15` + `useWorkshopState.ts:510-517` | `chapterNum` 与 Finalize/Expand/Humanizer/Batch 共享;改 chapterNum 后点 💾 保存,会把第 5 章内容存为第 6 章 | 生成时锁定 `savedChapterNum`,按钮标 "保存到第 N 章" |
| S6 | 工坊 | `WorkshopView.vue:60-73` + `WorkshopStepper.vue` | 第 7 步「续写」滚动到 BatchGenerate(不是续写流),用户点续写后找不到入口 | 给续写 `<details>` 加 `id="continue-anchor"` 并自动展开 |
| S7 | 漫剧 | `api/routers/manju.py:1496-1538` (`generate_manju_image`) | 漫剧分镜图保存到 `manju/images/` 但**未注册到 `add_image_record`**,导致 ImageView「生成记录」看不到,无法批量管理 | 在 `save_generated_image` 后调用 `add_image_record(...)` |
| S8 | 漫剧 | `ManjuView.vue:14, 1183-1189` | `ShotImageInline.vue` 已导入但**未使用**;分镜表格只显示文字 "预览/下载",**用户无法内联看图** | 操作列替换为 `<ShotImageInline>` |
| S9 | 漫剧 | `api/routers/manju.py:1268-1279` + `ManjuView.vue:588-598` | `bulkShotImportToQueue` 提交 `shot_ids` 但 `_manju_prompt_items` 忽略过滤,**全量导入而非选中导入** | `_manju_prompt_items` 增加 `shot_ids` 过滤 |
| S10 | 漫剧 | `ManjuView.vue:518-527` (`stepAnchors`) | ManjuSteps 第 7 步 `export` 锚点不存在 → 死按钮 | 加 `id="manju-export"` 或移除该步 |
| S11 | 漫剧 | `api/routers/manju.py:1541-1730` | 字符/场景/分镜生成耗时数小时,无 resume — SSE 一断就从第 1 章重跑 | 持久化 `progress.json`(last completed chapter),`generate_*_sync` skip 已完成段 |
| S12 | 漫剧 | `ManjuView.vue:449-463` (`regenerateShot`) | 无 try/catch,失败后 `dataMsg` 永驻 "正在重写分镜…" | 包 try/catch + `feedback.error` |
| S13 | 漫剧 | `useManjuHistory.ts:28` (`writeAll`) | 80 章脚本 JSON 写入 localStorage 经常超 5MB 配额,**回滚功能其实没存进去** | 移到服务端 `/manju/history` 端点;或截断 + 警告 |
| S14 | 配置 | `frontend/src/components/config/templates.ts:34-44` | 推荐 OpenAI 模板写的是 `gpt-5.4-mini` / `gpt-5.4` — **不存在的模型**,首次用户点 "测试连接" 必报错 | 改为 `gpt-4o-mini` / `gpt-4.1-mini` 等已知模型 |
| S15 | 配置 | `LLMConfigPanel.vue:64-69` (Embedding/Image 同) | 应用推荐模板时 `{...empty(), ...form.value, ...v}`,**旧表单的 API Key 会渗入新模板** | 改为 `form.value = { ...empty(), ...v }` |
| S16 | 配置 | `api/routers/config.py:409-459` (`extract_user_preferences`) | LLM 未配置时静默返回 `{"preferences": ""}`;profile-extract bar 永远无输出 | 加显式错误反馈或在未配 LLM 时禁用按钮 |
| S17 | 配置 | `api/routers/config.py:245-254` (`set_default_style`) | 接受裸 `dict`,任意字段会持久化进 `config.json` | 改用 pydantic schema |
| S18 | Shell | `App.vue:172-176` (`<keep-alive>`) | 仅 3 个 view 监听 `projectStore.filepath`,**切换项目后漫剧/工坊/图像看到旧项目数据,可能存错地方** | `<router-view :key="route.path + '@' + activeProject">` 一行修;或所有 view 都监听 filepath |
| S19 | Shell | `OnboardingWizard.vue:79-84` | `goConfig()` 用 `window.location.hash`/`pathname`,会全页刷新 + 清空 store + 可能死循环 | 改用 `router.push('/config')` |
| S20 | Shell | `stores/project.ts:69-96` | 切项目/删项目不中止运行中的 SSE,任务继续往死项目写;`generate.ts` 缓存不清 | activate/delete 时 abort 所有 task + 清缓存 |
| S21 | Shell | `stores/project.ts:49` | 无活动项目时 `filepath` 默认 `./output`,FilesView/KnowledgeView 会列出所有项目共享根目录 | 无活动项目时返回 `''`,view 显示 "请先选择项目" |
| S22 | Shell | `LogsView.vue:220-242` | Prompt 历史绕过 `logsApi` 用裸 axios,**带 token 时静默 401**,`clearPrompts` 假装清空 | 改用 `logsApi.getPrompts` / `clearPrompts` |

---

## 🟡 中等 (P1) — 摩擦 / 不一致 / 缺关键提示

| # | 模块 | 位置 | 问题 | 建议修复 |
|---|------|------|------|----------|
| M1 | 工坊 | `WorkshopView.vue:99-101` | "合并导出小说" 无章节时也能点 | 章节数 === 0 时禁用 |
| M2 | 工坊 | `StyleSelector.vue:16-19, 32-36` | DNA/style 分类靠 `name.includes('dna')` 字符串嗅探;且全局选中会**覆盖三步独立设置** | 服务端标 metadata;CTA 改 "应用到所有步骤"(显式) |
| M3 | 工坊 | `useWorkshopState.ts:420-441` (`runSSE`) | `cancelSSE` 不重置 `running=false`,按钮永久 "生成中…" | `cancelSSE` 内加 `state.running=false` |
| M4 | 工坊 | `ChapterStep.vue:65-76` | StreamOutput 可编辑但无脏标识;切到 Finalize 编辑被静默吞掉 | 加 "● 未保存" 徽标 |
| M5 | 工坊 | `ExpandStep.vue:233-289` | JSON 解析失败时 `expandNew` 空、`result` 也被清空,用户什么都看不见 | 解析失败时保留 `result` + 提示 "无法解析对比" |
| M6 | 工坊 | `BrainstormView.vue:113-118` | Enter 发送不检查 `e.isComposing`,中文 IME 误发 | `if (e.isComposing) return` |
| M7 | 工坊 | `CarryForward.vue:30-37` | `carryArchToBlueprint` 在 userGuidance 非空时悄悄 no-op,按钮仍亮 | userGuidance 有值时禁用按钮 |
| M8 | 漫剧 | `ManjuView.vue:419-447, 449-463, 333-343` | 大量子流程靠单一 `dataMsg` 显示状态,跨成功/错误/进行中闪烁 | 全量改 `feedback.*` |
| M9 | 漫剧 | `ManjuView.vue:154-183` (`loadStatus`) | `catch { /* ignore */ }`,后端 5xx 时只显空白无诊断 | `feedback.error` |
| M10 | 漫剧 | `ManjuView.vue:148, 894-901` (`canGenerate`) | 脚本步骤也强依赖 `appliedSettings`,但脚本只用 `scriptBody()` | 拆 `canRunScript` / `canRunDownstream` |
| M11 | 图像 | `ImageView.vue:137-197 (`batchGenerate`) + 290-343` | 无成本/时长预估;并发上限 10 没有 429 退避;失败列表不能批量重试 | 预估 banner;429 自动降速;失败重试入口 |
| M12 | 漫剧 | `api/routers/manju.py:1417-1443` (`continuity_check`) | 一致性检查重建 prompt,忽略用户编辑过的 `prompt_positive` | 直接对 `row["prompt_positive"]` 跑 `_quality_flags` |
| M13 | 漫剧 | `api/routers/images.py:131-173` | `batch_delete_*` 接受裸 `dict`,无 schema 无上限 | 引入 `BatchDeleteRequest(ids, max_items)` |
| M14 | 漫剧 | `manju.py` 中 dead | `PUT /manju/image-config` 与 client.ts 中 `saveImageConfig` 已无调用方;legacy `prompt_queue.json` 迁移代码也已无 UI | 移除 endpoint + 客户端封装 |
| M15 | 漫剧 | 图像 | 任何 SSE 任务都没有 UI "停止" 按钮(`sseHandle` 存了但没用) | 在 "生成中…" 旁加停止按钮 |
| M16 | 配置 | `StylesView.vue:519-521` | `onMounted` 串行 N+1 加载所有 style detail,20+ style 加载需数秒 | `Promise.all` 或一次性 `/styles?expand=detail` |
| M17 | 配置 | `StylesView.vue:77-261, 781-883` | "作者参考库" 实际是 KnowledgeView 复刻 1060 行 | 抽 `<AuthorReferencePanel :style-name>` |
| M18 | 配置 | `PresetsView.vue` vs `InstructionConfigView.vue` | 两个 ~500 行的近相同 3 栏编辑器(库/预览/应用) | 抽 `<PromptTemplateEditor :source>` |
| M19 | 配置 | `xp_presets` vs `presets` | 命名冲突,语义完全不同(prompt 套件 vs XP 偏好片段) | `xp_presets` 改名 `kink_presets`/`xp_traits`,或并入 Profile |
| M20 | 配置 | `ConfigView.vue:11-22` | 应用模板会无 dirty-check 覆盖已编辑表单 | 应用前 dirty-check 提示 |
| M21 | 配置 | `StylesView.vue:412-428` vs `442-457` | "应用到当前项目" 和 "设为全局默认" 视觉权重相同,**误点会改全局默认** | 全局默认改为 outlined-danger + 确认弹窗 |
| M22 | 配置 | `ProfileView.vue:46-67` | 单一全局 textarea 应用到所有项目,与 xp_presets 数据模型割裂 | 链接两者或统一数据模型 |
| M23 | 配置 | `InstructionConfigView.vue:167-169` | "返回漫剧制作" 按钮即使直接访问页面也显示 | 改为根据 history 动态;或移除 |
| M24 | 配置 | `ImageConfigPanel.vue:95-103` | 测试连接固定用 `1024x1024 / low`,绕过已保存参数 | 用保存参数测试;或重命名 "快速联通性测试" |
| M25 | 配置 | `OnboardingWizard.vue:23-26` | 只要求 LLM + 项目,不提示 Embedding/Image | Onboarding 列出三个模型组(可跳过) |
| M26 | Shell | `App.vue:23-24` | 通过 `window.__nwFeedback` / `__nwTasks` 暴露给 `api/client.ts`,HMR 时容易丢 | client.ts 直接 import store |
| M27 | Shell | 多处 14 处 `confirm()` | 不一致;`stores/feedback.ts:35` 已有 undoFn 模式但没用 | 加 `ConfirmDialog` 组件,或 "先做后撤销" 模式 |
| M28 | Shell | `App.vue:55` | `bottomLinks` 硬编码,在 beginner mode 下少一项 | 根据 level 派生 |
| M29 | Shell | `App.vue:126` | 移动端 topbar 搜索按钮用 presets 图标 | 改为放大镜 |
| M30 | Shell | `OnboardingWizard.vue:167` | "完成" 按钮 disabled 时不说明缺什么 | 旁边列出 checklist |
| M31 | Shell | `composables/useAutoSelect.ts` | 53 行代码,**全项目无引用** — 4 个面板各自手写同款逻辑 | 接入或删除 |
| M32 | Shell | `stores/feedback.ts:24-27` | error 不自动消失,20 条 FIFO 顶掉历史;无 dismissAll | 同消息聚合 + dismissAll |
| M33 | Shell | `stores/generate.ts:1-48` | 缓存 architecture/blueprint 等内容,但 FilesView 编辑同名文件后不失效 | 路由 enter 时重新 loadStatus;或 FilesView 写入后广播 invalidate |
| M34 | Shell | `LogsView.vue:65-69` | 模块检测正则只匹配英文 module 名,中文日志全归 `system` | 解析 Python logger.name 而非消息文本 |
| M35 | Shell | `LogsView.vue:377` | 智能提示里的 "前往「模型配置」" 是纯文本,不可点 | 改 `router.push('/config')` |

---

## 🟢 轻微 (P2) — 抛光 / 文案 / 可访问性

| # | 模块 | 位置 | 问题 |
|---|------|------|------|
| L1 | 工坊 | `WorkshopView.vue:130` | StyleSelector 关闭后无法重开 |
| L2 | 工坊 | `DraftRestoreBanner.vue:9-14` | `fmt()` 不处理 > 1 天,2 天前显 "1 小时前" |
| L3 | 工坊 | `StyleSelector.vue:84-86` | 空状态提到的「文风与DNA」不是真链接 |
| L4 | 工坊 | `ProjectBar.vue:43` | "已切换到" toast 在 `reloadProjectContent` 完成前就吐 |
| L5 | 工坊 | `ConsistencyView.vue:14-27` | 进度仅显原始 msg,无章节上下文 |
| L6 | 工坊 | `PreflightCheck.vue:24, 36` | "请先完成架构生成" 不读 `generateStore.architectureContent`,刷新后误报 |
| L7 | 工坊 | `useWorkshopState.ts:532-564` | `doGenerateArch` 不设 startedAt/endedAt,ResultSummary 没耗时 |
| L8 | 工坊 | `BatchGenerate.vue:13` | 文案 "步骤3中所选的文风" 对不上 stepper 命名 |
| L9 | 工坊 | `useWorkshopState.ts:680-710` | 末批次生成后按钮变 "▶ 生成第 N+1 - N 章细纲" 死状态 |
| L10 | 漫剧 | `ManjuView.vue:129-145` + `ManjuSteps.vue` | 两套 stepper 状态重复 |
| L11 | 漫剧 | `PromptTemplatePreview.vue:14-36` | 显示手写 mock 而非真实模板 |
| L12 | 漫剧 | `ContinuityIssues.vue:76` | 空态分不清 "无问题" 还是 "未运行" |
| L13 | 漫剧 | `ManjuView.vue:1212` | `<router-link to="/images">` 丢失漫剧表单状态 |
| L14 | 图像 | `ImageView.vue:19-20` | `tab` 不持久化,刷新回到队列 |
| L15 | 漫剧 | `ManjuSteps.vue:33` | "导出" 步骤 `done: false` 硬编码 |
| L16 | 漫剧 | `ManjuView.vue:796,864` | `importMsg`/`settingsMsg` 用 `startsWith('✅')` 判颜色 |
| L17 | 配置 | `useConfigHealth.ts:54-67` | 健康状态存 localStorage,换浏览器后全是 "未测试" |
| L18 | 配置 | `StylesView.vue:554-578` | 三块说明永久显示,无折叠 |
| L19 | 配置 | `PresetsView.vue:175,199-209` | 用原生 `confirm()` |
| L20 | 配置 | `useConfigValidation.ts:20-22` | "末尾 / 可能问题" 文案误导(OpenAI base URL 通常应以 `/v1` 结尾) |
| L21 | 配置 | `api/routers/styles.py:117-131` | `get_style` 绕过 `app.load_style` 直接打开 JSON |
| L22 | 配置 | `RecommendedTemplates.vue:42-44` | 卡片无 aria-label,纯点击 |
| L23 | Shell | `CommandPalette.vue:42` | 14 路由列表三处重复(router.ts + App.vue + CommandPalette) |
| L24 | Shell | `TaskBar.vue:39` | "清除已结束" 内联闭包变量,可抽 `tasks.clearFinished()` |
| L25 | Shell | `FeedbackCenter.vue:38` | 移动端与 TaskBar 和底部导航重叠 |
| L26 | Shell | `FilesView.vue:23-46` | 大量 ref 可抽 `useFilesState` |
| L27 | Shell | `api/routers/files.py:22` | 白名单 `.txt .json .md` 未在 UI 说明 |
| L28 | Shell | `api/routers/logs.py:14` | prompt 历史是 workspace 级而非项目级,与其他模块语义不一致 |

---

## 💡 建议 (架构级,需更长讨论)

| # | 建议 | 影响范围 |
|---|------|----------|
| A1 | 在 ChapterStep 加 "加载章节" 入口 | 填补最大的功能缺口 |
| A2 | 引入 `useChapterCursor()` 统一管理 `chapterNum / outlineBatchStart / humanizerStart/End` | 消除章节范围多源不同步 |
| A3 | 拆分 `manju.py`(1766 行)为 5 个子模块:parser / prompts / pipeline / export / images | 易维护 |
| A4 | 将漫剧分镜图通过 `images.py` 派发,出现在 RecordsTab | 统一两套图像流 |
| A5 | 服务端持久化 useManjuHistory(已有 manju/ 目录) | localStorage 不够大且不可靠 |
| A6 | 合并 `xp_presets` 进 ProfileView 作 "命名片段" tab | 消除最严重命名冲突 |
| A7 | 抽 `<LibraryManager :scope>` 同时供 KnowledgeView 与 StylesView 的作者库使用 | 消除 ~700 行重复 |
| A8 | 合并 `PresetsView` 与 `InstructionConfigView` 为 `<PromptTemplateEditor :source>` | 消除 ~700 行重复 |
| A9 | 服务端连通性探针 `/config/health`,顶栏单一红点指示器 | 替代 N 个分散 banner |
| A10 | 推荐模板锁在服务端 manifest(`/config/recommended-templates`) | 模型名更新不需重发布前端 |
| A11 | `router.beforeEach` 强制 active project 守卫 + "选择/创建项目" 占位页 | 消除 14 view 各自防御 |
| A12 | 统一 `feedback.*`,lint 禁 `alert()`/`confirm()`,提供 `ui.confirm({...})` | 一致性 |
| A13 | 移除 `window.__nw*` 桥,client.ts 直接 import store | 移除脆弱逃生口 |
| A14 | 路由配置加 `meta: { group, level, icon }` 字段,sidebar + CommandPalette + bottom nav 同源 | 消除三处重复 |

---

## 修复优先级建议

第一批(本轮做):**所有 P0 中前 10 个 + 几个高价值 P1**,影响一线用户体验且改动小。
第二批:**剩余 P0 + 集中处理 P1**。
第三批:**P2 抛光**(批量做,1-2 commit 收尾)。
第四批:**架构建议**,需逐项讨论后再动。
